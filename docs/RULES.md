# Rules

## Design Rules
Edit `backend/rules/design_rules.yaml` to adjust thresholds and recommended designs.

## Validation Rules
Edit `backend/rules/validation_rules.yaml` to control units and ranges for PK metrics.

## Variability Rules
Edit `backend/rules/variability_rules.yaml` to adjust base CV ranges by BCS class.

## Regulatory Checks
Edit `backend/rules/reg_rules.yaml` to document checklist items.
